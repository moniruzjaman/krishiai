apiVersion: v1
kind: Secret
metadata:
  name: krishi-ai-secrets
  namespace: krishi-ai
  labels:
    app.kubernetes.io/name: krishi-ai
    app.kubernetes.io/component: secrets
type: Opaque
# Values should be set using scripts/setup-secrets.sh or overridden per environment.
# These are placeholder base64-encoded values — replace before production use.
data:
  SECRET_KEY: "REPLACE_WITH_BASE64_ENCODED_VALUE"
  JWT_SECRET: "REPLACE_WITH_BASE64_ENCODED_VALUE"
  DATABASE_PASSWORD: "REPLACE_WITH_BASE64_ENCODED_VALUE"
  POSTGRES_PASSWORD: "REPLACE_WITH_BASE64_ENCODED_VALUE"
  GEMINI_API_KEY: "REPLACE_WITH_BASE64_ENCODED_VALUE"
  OPENAI_API_KEY: "REPLACE_WITH_BASE64_ENCODED_VALUE"
